# 쇼츠 소재 분석 자동화 프로그램 — 기획서 (PRD)

> 이 문서는 Claude Code에 그대로 입력해 로컬에서 개발하기 위한 기획/설계 문서입니다.
> 코드는 포함하지 않으며, "무엇을 / 왜 / 어떤 순서로" 만들지를 정의합니다.

---

## 1. 한 줄 정의

유튜브 영상 URL 하나를 넣으면, **실제 시청 데이터 + 댓글 반응 + 자막**을 결합 분석해서
"쇼츠로 만들 최적 구간 + 썸네일/제목 후보 + 편집 가이드"를 자동으로 뽑아주는 **로컬 웹앱 (Node.js)**.

> 브라우저에서 URL 붙여넣고 버튼 누르면 → 서버가 분석 → 결과를 웹 화면에 보여주는 형태.
> 로컬(내 컴퓨터)에서 `localhost`로 띄워서 쓴다.

---

## 2. 왜 만드는가 (차별점)

기존 도구(Opus Clip 등)는 **AI가 자막만 보고 추측한 "바이럴 점수"**를 줍니다. 추측입니다.
이 프로그램은 **추측이 아니라 실제 신호 3종**을 근거로 삼습니다.

| 신호 | 출처 | 의미 |
|------|------|------|
| ① 댓글 속 타임스탬프 (최우선) | 댓글 본문 파싱 | 시청자가 직접 "○분○초 여기 웃김"이라고 지목한 명장면 |
| ② Most Replayed 히트맵 (보강) | yt-dlp `heatmap` 필드 | 진짜 사람들이 가장 많이 다시 본 구간 (실데이터) |
| ③ 자막 + 감정/유머 분석 | 자막 + LLM | 그 구간에서 "무슨 말/행동"이 나왔는지 |

→ ①번이 이 프로그램의 심장. 사람이 직접 손가락으로 가리킨 구간이라 가장 정확함.
→ ①과 ②가 겹치면 "사람도 지목 + 데이터도 증명" = 최강 후보.

---

## 3. 핵심 사용자 흐름 (User Flow)

1. 사용자가 유튜브 URL을 입력한다.
2. 프로그램이 자동으로 데이터를 수집한다 (영상 메타·히트맵·댓글·자막).
3. 세 신호를 하나의 타임라인 위에 정렬·결합한다.
4. LLM이 결합 데이터를 분석해 "쇼츠 후보 구간"을 점수순으로 선별한다.
5. 각 후보마다 [시작~끝 타임코드 / 선정 이유 / 썸네일 멘트 / 제목 후보 3개 / 편집 노트]를 생성한다.
6. 결과를 리포트(.md 또는 .json)로 저장한다.
7. (옵션) 선택한 후보를 ffmpeg로 실제 클립 파일로 잘라낸다.

---

## 4. 시스템 구조 (웹앱 = 프론트엔드 + 백엔드)

> 웹앱이므로 "브라우저(화면)"와 "서버(처리)"가 분리됨. Claude Code에 이 경계를 명확히 알려줄 것.

```
┌─────────────────────────────────────────────┐
│  [프론트엔드 / 브라우저]                       │
│   - URL 입력창 + 분석 버튼                     │
│   - 진행 상태 표시 (수집중→분석중→완료)         │
│   - 결과 카드(핫존별 썸네일멘트/제목/편집노트)   │
└───────────────┬─────────────────────────────┘
                │ HTTP 요청 (POST /analyze {url})
                ▼
┌─────────────────────────────────────────────┐
│  [백엔드 / Node.js 서버]                       │
│                                               │
│  ① Collector (수집)                           │
│     - yt-dlp를 자식 프로세스로 실행            │
│       (Node가 yt-dlp 명령어를 호출)            │
│     - 댓글 대량 수집 / 히트맵 / 자막            │
│        │                                      │
│        ▼                                      │
│  ② Normalizer (정규화)                        │
│     - 댓글 타임스탬프 정규식 추출(JS)          │
│     - 초 단위 통일 + 좋아요/빈도 가중치        │
│        │                                      │
│        ▼                                      │
│  ③ SignalMerger (신호 결합)                   │
│     - 댓글 클러스터 + 히트맵 → 핫존 산출       │
│        │                                      │
│        ▼                                      │
│  ④ Analyzer (LLM 분석)                        │
│     - Claude API 호출 → 썸네일/제목/편집노트   │
│        │                                      │
│        ▼                                      │
│  ⑤ Responder                                  │
│     - 결과 JSON을 프론트로 응답               │
│     - (옵션) ffmpeg로 클립 컷                 │
└─────────────────────────────────────────────┘
```

**중요 포인트:** yt-dlp와 ffmpeg는 Node 라이브러리가 아니라 **외부 실행 파일**임.
Node의 `child_process`로 명령어를 실행하고 결과(stdout/파일)를 받아오는 구조.
→ 사용 전 시스템에 yt-dlp, ffmpeg가 설치돼 있어야 함.

---

## 5. 모듈별 상세 명세

### ① Collector — 데이터 수집
- **영상 메타·히트맵·자막·다운로드**: `yt-dlp` 사용.
  - 핵심: `yt-dlp`는 영상 정보(JSON)에 `heatmap` 필드를 제공함 → **이게 Most Replayed 데이터**.
  - 자동 자막을 타임스탬프 포함 형식(`.vtt`/`.srt`)으로 받기.
- **댓글 수집 — ⚠️ 목표 재정의**:
  - "인기 댓글을 가져오는 것"이 목표가 **아님**.
  - 진짜 목표 = **본문에 타임스탬프가 박힌 댓글만 골라내는 것** (예: "3:20 여기 개웃김ㅋㅋ", "1:05 소름").
  - 따라서 댓글은 **인기순 일부만**이 아니라 **가능한 한 많이(전체에 가깝게)** 수집해야 함.
    이유: 타임스탬프 댓글은 전체 댓글 중 소수라서, 적게 긁으면 핵심 신호를 놓침.
  - 수집 방식:
    - (A) **`yt-dlp`를 `child_process`로 실행** — API 키 불필요, **전체 댓글에 가깝게 긁기 좋음**(쿼터 제한 없음). 대량 수집엔 이쪽이 유리. **권장.**
      - Node에서 `yt-dlp --write-comments --skip-download` 같은 명령으로 댓글을 JSON으로 받아 파싱.
      - 또는 npm 래퍼 `youtube-dl-exec`로 깔끔하게 호출.
    - (B) YouTube Data API v3 `commentThreads` — 빠르지만 쿼터 제한으로 대량 수집 시 한계.
  - **수집 직후 1차 필터링**: 정규식으로 타임스탬프가 포함된 댓글만 남기고 나머지는 버림 → 데이터량 급감, 다음 단계 가벼워짐.
- 출력: 표준화된 중간 데이터 파일(`raw_data.json`)로 저장 → 다음 모듈이 이걸 읽음 (단계 분리).
  - 댓글은 `[타임스탬프(초), 댓글원문, 좋아요수]` 형태로만 저장.

### ② Normalizer — 정규화 (이 프로그램의 1차 핵심 로직)
- 자막의 `00:03:20.500` 같은 표기를 모두 **정수 초(second)** 로 변환.
- **댓글 타임스탬프 추출 — 가장 중요한 파싱 로직:**
  - 정규식으로 댓글 본문에서 시각 표기를 모두 뽑아냄:
    - 패턴: `(?:(\d{1,2}):)?(\d{1,2}):(\d{2})` → `3:20`(분:초), `1:05:30`(시:분:초) 모두 커버.
  - 한 댓글에 여러 타임스탬프가 있으면 각각 분리해서 카운트 (예: "1:20 웃기고 3:45 더 웃김").
  - 추출한 표기를 **초 단위로 변환** (3:20 → 200초).
  - **노이즈 거르기**: 영상 길이를 초과하는 값, 0:00, 같은 댓글의 중복 표기 등은 제외.
- 각 타임스탬프 신호에 **가중치** 부여:
  - 그 댓글의 **좋아요 수** = 기본 가중치 (좋아요 많은 댓글이 지목한 구간 = 신뢰도↑).
  - **여러 사람이 같은 구간을 지목한 횟수**(빈도) = 추가 가중치. → 결국 "많은 사람이 + 좋아요 받으며 지목한 구간"이 최강 신호.
  - (옵션) 댓글에 웃음/감탄 표현("ㅋㅋ", "ㅎㅎ", "개웃", "소름", "미쳤" 등)이 같이 있으면 "유머/임팩트 태그"로 분류해 저장 → 나중에 LLM이 톤 분석에 활용.

### ③ SignalMerger — 신호 결합
- **댓글 타임스탬프 클러스터링 (1순위 신호):**
  - 비슷한 시각(±10~15초)에 몰린 댓글 타임스탬프를 하나의 클러스터로 묶음.
  - 클러스터별로 [지목 횟수 + 좋아요 가중 합]을 점수화 → **사람들이 직접 "여기"라고 외친 구간**.
- **히트맵 피크 탐지 (보강 신호):** 히트맵 값이 평균 + (1~1.5 표준편차) 이상인 구간을 피크로 마킹.
- **핫존 산출:** 댓글 클러스터를 중심에 두고, 히트맵 피크와 겹치면 가중치 대폭 상승 (실데이터 + 사람 지목 = 확실).
  - 댓글 신호만 있고 히트맵이 없어도 핫존으로 채택 (댓글이 더 직접적인 "재밌다" 신호이므로).
- 각 핫존에 해당 시간대의 **자막 텍스트**를 붙여서 "그 구간에서 무슨 일이 있었는지" 컨텍스트 완성.
- 각 핫존에 **그 구간을 지목한 실제 댓글 원문들**도 함께 묶기 (LLM이 "사람들이 뭐라고 반응했는지" 직접 읽도록).
- 출력: `hot_zones.json` (구간별 시작초/끝초/댓글점수/히트맵점수/자막/관련댓글원문).

### ④ Analyzer — LLM 분석 (이 프로그램의 두뇌)
- 각 핫존의 [자막 + 관련 댓글 모음 + 신호 점수]를 **Claude API**(Anthropic SDK)에 전달.
- LLM에게 시킬 일:
  1. 이 구간이 왜 반응이 좋은지 한 줄 분석 (유머/반전/정보/감정 중 분류)
  2. 쇼츠용 **최적 컷 시작~끝** 미세 조정 추천 (예: 임팩트 1.5초 전부터 시작)
  3. **썸네일 멘트** 1개 (8자 이내, 강한 후크)
  4. **제목 후보 3개** (한국어, 클릭 유발형)
  5. **편집 노트** (자막 강조 포인트, 효과음/줌인 추천 타이밍)
- 출력은 반드시 **JSON 형식 강제** (파싱 안정성).

### ⑤ Responder — 출력 (웹 응답)
- 분석 결과(핫존 + 분석)를 **JSON으로 프론트엔드에 응답**.
- 프론트는 이걸 받아 **핫존별 카드**로 렌더링: [구간 타임코드 / 점수 / 선정 이유 / 썸네일 멘트 / 제목 후보 3개 / 편집 노트 / 지목한 실제 댓글들].
- (옵션) "이 리포트 저장" 버튼 → `.md`/`.json` 다운로드.
- (옵션) **ffmpeg 자동 컷**: 카드의 "클립 만들기" 버튼 → 서버가 ffmpeg로 시작~끝 구간을 세로(9:16) 크롭해 클립 파일 생성 → 다운로드 링크 제공.
  - 단, 자동 자막 입히기/템플릿은 1차 범위에서 제외(Vrew 등 전용 툴이 더 나음).

---

## 6. 기술 스택 제안 (Node.js 웹앱)

| 영역 | 추천 | 비고 |
|------|------|------|
| 런타임 | Node.js (LTS) | |
| 백엔드 프레임워크 | Express (가벼움) 또는 Next.js (프론트+백 통합) | 입문엔 Express, 통합 원하면 Next.js |
| 프론트엔드 | React (Next.js 쓰면 자동) 또는 순수 HTML+JS | 화면이 단순하면 후자도 OK |
| 영상/자막/히트맵/댓글 | **yt-dlp** (외부 실행) | `child_process`로 호출. Node용 wrapper(`youtube-dl-exec` 등) 써도 됨 |
| 타임스탬프 파싱 | JS 정규식 | 별도 라이브러리 불필요 |
| LLM 분석 | **Anthropic SDK (`@anthropic-ai/sdk`)** | 공식 Node SDK, JSON 출력 강제 |
| 영상 컷(옵션) | **ffmpeg** (외부 실행) | `fluent-ffmpeg` 래퍼 권장 |
| 비동기 작업 처리 | 단순: async/await로 직결 / 본격: 작업 큐(BullMQ 등) | MVP는 그냥 async로 충분 |
| 진행상태 표시 | 폴링 또는 SSE(Server-Sent Events) | 분석이 길어지므로 "처리중" UX 필요 |

> **핵심 의존성 주의:** yt-dlp와 ffmpeg는 npm 패키지가 아니라 시스템에 별도 설치하는 프로그램.
> npm 래퍼(`youtube-dl-exec`, `fluent-ffmpeg`)는 그 프로그램을 편하게 호출해주는 보조일 뿐, 본체는 따로 설치해야 함.

---

## 7. 개발 단계 (Claude Code에 이 순서로 시키면 됨)

**MVP (1차) — 분석까지만, 영상 컷 제외**
0. Node 프로젝트 셋업 + Express 서버 + URL 입력 화면(기본 HTML) + `/analyze` 엔드포인트 골격
1. yt-dlp를 child_process로 호출해 **댓글 대량 수집** → 타임스탬프 포함 댓글만 1차 필터링 (**최우선 핵심**)
2. Normalizer: 댓글에서 타임스탬프 정규식 추출 + 초 단위 변환 + 좋아요/빈도 가중치
3. SignalMerger: 댓글 타임스탬프 클러스터링 → 핫존 산출
4. yt-dlp로 히트맵 + 자막 수집 → 핫존에 자막·히트맵 보강 결합
5. Analyzer: Anthropic SDK로 구간별 썸네일/제목/편집노트 생성
6. Responder: 결과 JSON을 프론트로 응답 → 화면에 카드로 렌더링
→ **1~3단계(댓글 타임스탬프 파싱)가 이 프로그램의 진짜 가치. 이것부터 완성.**

**2차 확장**
7. ffmpeg 자동 클립 컷 (9:16)
8. 여러 영상 일괄 처리 (배치)
9. 결과를 표/대시보드로 보기 좋게

**3차 (나중)**
10. 경쟁 채널 인기 쇼츠 패턴 자동 수집·학습
11. 내가 만든 쇼츠 성과 피드백 루프

---

## 8. Claude Code 첫 지시 예시 (그대로 복붙용)

> "이 PRD를 기반으로 Node.js 웹앱을 만들자. 먼저 7번 MVP의 0~2단계만 구현해줘.
> Express로 서버를 띄우고, 브라우저에서 유튜브 URL을 입력해 분석 버튼을 누르면 `/analyze`로 POST되게 해줘.
> 서버는 yt-dlp를 child_process로 실행해 그 영상의 댓글을 최대한 많이 수집하고,
> 본문에 타임스탬프(예: 3:20, 1:05:30)가 들어있는 댓글만 정규식으로 골라내서
> [타임스탬프(초), 댓글원문, 좋아요수] 형태로 만든 뒤, 일단 그 결과를 화면에 표로 보여주는 것까지만 해줘.
> 파일은 server.js / collector.js / normalizer.js 로 모듈을 분리하고,
> yt-dlp와 ffmpeg가 시스템에 설치돼 있어야 한다는 점을 README에 적어줘."

→ 한 단계씩 끊어서 시키는 게 핵심. 한 번에 다 시키면 디버깅이 지옥됨.

---

## 9. 리스크 & 주의사항

- **저작권/권리**: 남의 영상을 잘라 쇼츠로 올리는 건 채널마다 정책·법적 회색지대가 있음.
  본인 영상이거나, 출처 표기·변형(리액션/해설 추가) 전제로 활용 권장.
- **유튜브 ToS**: 대량 스크래핑은 ToS 위반 소지. 개인·소량·로컬 분석 용도로 한정.
- **히트맵 부재**: 조회수가 적은 영상은 Most Replayed 데이터가 없을 수 있음 → 이 경우 댓글+자막 신호로만 동작하도록 fallback 설계.
- **API 비용**: Claude API는 영상당 호출량에 비례. 핫존만 추려서 분석(전체 자막 통째로 X)해 토큰 절약.
- **댓글 타임스탬프 노이즈**: 광고·도배 댓글 필터링 로직 필요(좋아요 0 + 반복 텍스트 제외).
- **외부 의존성 설치**: yt-dlp, ffmpeg는 npm install로 안 깔림. 시스템에 별도 설치 필요(README에 명시). 미설치 시 child_process 호출이 에러남 → 친절한 에러 메시지 처리.
- **비동기/타임아웃**: 댓글 대량 수집은 수십 초~수 분 걸릴 수 있음. HTTP 요청이 타임아웃 나지 않게 진행상태 표시(SSE/폴링) 또는 작업 큐 고려. MVP는 일단 로딩 스피너 + 넉넉한 타임아웃으로 충분.
- **API 키 보안**: Anthropic API 키는 절대 프론트엔드 코드에 넣지 말 것. 반드시 서버(.env)에서만 사용.

---

## 10. 성공 기준 (이걸로 완성도 판단)

- [ ] URL 하나로 분석 리포트가 자동 생성된다.
- [ ] 추천 구간이 "실제 Most Replayed 피크"와 일치한다.
- [ ] 썸네일 멘트·제목이 그대로 써도 될 만큼 자연스럽다.
- [ ] 분석 1회당 30초~2분 내 완료.
